let element = document.querySelector('.pm');
let classList = element.classList; // получаем список классов элемента
let classCount = classList.length; // считаем количество классов
console.log(`Количество классов: ${classCount}`);